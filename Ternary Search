#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int pieces(vector<double>& ropes, double length) {
    int count = 0;
    for (double r : ropes)
        count += (int)(r / length);
    return count;
}

int main() {

    vector<double> ropes = {8.0, 7.0, 5.0};
    int k = 5;

    double left = 0.0;
    double right = *max_element(ropes.begin(), ropes.end());

    for (int i = 0; i < 100; i++) {   // iterate for precision
        double mid1 = left + (right - left) / 3;
        double mid2 = right - (right - left) / 3;

        if (pieces(ropes, mid1) >= pieces(ropes, mid2))
            right = mid2;
        else
            left = mid1;
    }

    cout << "Maximum length: " << left << endl;

    return 0;
}